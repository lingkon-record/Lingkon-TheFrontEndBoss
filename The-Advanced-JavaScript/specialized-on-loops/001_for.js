// Define an array of numbers
const numbers = [10,5,20,50,100];

// Use a for loop to iterate over the array
for (let i = 0; i < numbers.length; i++) {
  // Access the current element using numbers[i]
  console.log(numbers[i]);
}
